{
  "version": 2,
  "buildCommand": "pnpm -w run vercel-build",
  "outputDirectory": "artifacts/liquidity-radar/dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/server" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/server.ts": {
      "maxDuration": 30
    }
  }
}
